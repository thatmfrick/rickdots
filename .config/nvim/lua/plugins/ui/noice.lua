require("notify").setup({
        background_colour = "#ce7e00",
        timeout = 1000,
        render = "compact",
        fps = 240,
        stages = "fade",
        level = 2,
        top_down = true
})
require("noice").setup({})
